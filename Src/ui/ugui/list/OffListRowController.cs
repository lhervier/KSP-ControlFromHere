using UnityEngine;

namespace com.github.lhervier.ksp.controlfromheremod.ui.ugui.list
{
    /// <summary>
    /// Marker component of the off-list control-point row (pinned at the top of the list). Like
    /// <see cref="RowController"/> it holds no behavior: the row is static and rebuilt by the
    /// <see cref="ListController"/> whenever the vessel or its control point changes.
    /// </summary>
    public class OffListRowController : MonoBehaviour
    {
    }
}
